# A program that displays today's date
# use date object from the datetime library to access calendar information
# use the date.today()   use the method today() on the date object
 
from datetime import date 
print(date.today())
#print("Today's date is : " + str(date.today())) # convert date into str using str()

